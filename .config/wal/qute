# Autogenerated config.py
# Documentation:
#   qute://help/configuring.html
#   qute://help/settings.html

c.url.start_pages = 'file:///home/ringdal/Projekter/html/startskærm/index.html'
c.url.default_page = 'file:///home/ringdal/Projekter/html/startskærm/index.html'

c.colors.webpage.bg = '{color0}'

c.colors.completion.fg = '{color4}'
c.colors.completion.odd.bg = '{color0}'
c.colors.completion.even.bg = '{color0}'
c.colors.completion.category.fg = '{color0}'
c.colors.completion.category.bg = '{color4}'
c.colors.completion.item.selected.fg = '{color0}'
c.colors.completion.item.selected.bg = '{color4}'
c.colors.completion.item.selected.border.top = '{color4}'
c.colors.completion.item.selected.border.bottom = '{color4}'
c.colors.completion.item.selected.match.fg = '{color2}'
c.colors.completion.match.fg = '{color2}'
c.colors.completion.scrollbar.fg = '{color4}'
c.colors.completion.scrollbar.bg = '{color0}'

c.colors.statusbar.url.fg = '{color4}'
c.colors.statusbar.url.error.fg = '{color4}'
c.colors.statusbar.url.hover.fg = '{color4}'
c.colors.statusbar.url.warn.fg = '{color4}'
c.colors.statusbar.url.success.http.fg = '{color4}'
c.colors.statusbar.url.success.https.fg = '{color4}'
c.colors.statusbar.normal.bg = '{color0}'
c.colors.statusbar.normal.fg = '{color4}'

## TABS

c.tabs.title.alignment = 'center'
c.tabs.max_width = -1
c.tabs.position = 'top'
c.tabs.show = 'multiple'
c.tabs.show_switching_delay = 2000

c.colors.tabs.bar.bg = '{color4}'
c.colors.tabs.odd.bg = '{color4}'
c.colors.tabs.odd.fg = '{color0}'
c.colors.tabs.even.bg = '{color4}'
c.colors.tabs.even.fg = '{color0}'
c.colors.tabs.selected.odd.bg = '{color0}'
c.colors.tabs.selected.even.bg = '{color0}'
c.colors.tabs.selected.odd.fg = '{color4}'
c.colors.tabs.selected.even.fg = '{color4}'

c.colors.tabs.indicator.start = '{color4}'
c.colors.tabs.indicator.stop = '{color4}'

# Aliases for commands. The keys of the given dictionary are the
# aliases, while the values are the commands they map to.
# Type: Dict
c.aliases = {'w': 'session-save', 'q': 'quit', 'wq': 'quit --save'}

# Backend to use to display websites. qutebrowser supports two different
# web rendering engines / backends, QtWebKit and QtWebEngine. QtWebKit
# was discontinued by the Qt project with Qt 5.6, but picked up as a
# well maintained fork: https://github.com/annulen/webkit/wiki -
# qutebrowser only supports the fork. QtWebEngine is Qt's official
# successor to QtWebKit. It's slightly more resource hungry than
# QtWebKit and has a couple of missing features in qutebrowser, but is
# generally the preferred choice.
# Type: String
# Valid values:
#   - webengine: Use QtWebEngine (based on Chromium).
#   - webkit: Use QtWebKit (based on WebKit, similar to Safari).
c.backend = 'webengine'

# Enable JavaScript.
# Type: Bool
config.set('content.javascript.enabled', True, 'file://*')

# Enable JavaScript.
# Type: Bool
config.set('content.javascript.enabled', True, 'chrome://*/*')

# Enable JavaScript.
# Type: Bool
config.set('content.javascript.enabled', True, 'qute://*/*')

# List of user stylesheet filenames to use.
# Type: List of File, or File

# Duration (in milliseconds) to wait before removing finished downloads.
# If set to -1, downloads are never removed.
c.downloads.remove_finished = 2000

# Hide the statusbar unless a message is shown.
c.statusbar.show = 'in-mode'
c.statusbar.padding = {'top': 0, 'bottom': 0, 'left': 0, 'right': 0}

# Position of the status bar.
# Type: VerticalPosition
# Valid values:
#   - top
#   - bottom
c.statusbar.position = 'bottom'

# List of widgets displayed in the statusbar.
# Type: List of String
# Valid values:
#   - url: Current page URL.
#   - scroll: Percentage of the current page position like `10%`.
#   - scroll_raw: Raw percentage of the current page position like `10`.
#   - history: Display an arrow when possible to go back/forward in history.
#   - tabs: Current active tab, e.g. `2`.
#   - keypress: Display pressed keys when composing a vi command.
#   - progress: Progress bar for the current page loading.
c.statusbar.widgets = ['keypress', 'progress', 'url']
c.tabs.padding = {'top': 3, 'bottom': 3, 'left': 0, 'right': 0}


## FONTS

# Default monospace fonts. Whenever "monospace" is used in a font
# setting, it's replaced with the fonts listed here.
#c.fonts.monospace = 'SF Mono'

# Font used in the completion widget.
c.fonts.completion.entry = '9pt monospace'

# Font used for the debugging console.
c.fonts.debug_console = '9pt monospace'

# Font used for the downloadbar.
c.fonts.downloads = '9pt monospace'

# Font used in the keyhint widget.
c.fonts.keyhint = '9pt monospace'

# Font used for error messages.
c.fonts.messages.error = '9pt monospace'

# Font used for info messages.
c.fonts.messages.info = '9pt monospace'

# Font used for warning messages.
c.fonts.messages.warning = '9pt monospace'

# Font used in the statusbar.
c.fonts.statusbar = '9pt monospace'

# Font used in the tab bar.
#c.fonts.tabs = '9pt monospace'

# Default font size (in pixels) for regular text.
c.fonts.web.size.default = 17

# Default font size (in pixels) for fixed-pitch text.
c.fonts.web.size.default_fixed = 14

# c.content.user_stylesheets = ['style.css']
#
c.messages.timeout = 5000
# Bindings for normal mode
config.bind(',p', 'spawn --userscript qute-pass')
config.bind(',n', 'config-cycle content.user_stylesheets style.css "" ')
config.bind('td', 'hint links spawn transmission-remote -a {hint-url}')
config.bind(',hy', 'hint links spawn tmux new-window -t scratch -n:youtube-dl ydl qute {hint-url}')
config.bind(',y', 'spawn tmux new-window -t scratch -n:youtube-dl ydl qute {url}')
config.bind(',test', 'st -e spawn tmux new-window -t \"scratch\" -n:echo {url};sleep 5')
config.bind('tg', 'tab-give')
