#!/usr/bin/env bash
# Password askpass (same UI as zenity-askpass). Kept as a stable path for
# older callers; always prefers SUDO_DISPLAY_CMD so the real argv is shown.

set -euo pipefail
exec /home/ringdal/.Scripts/zenity-askpass "$@"
