#!/usr/bin/env bash

edit(){ fd .md $NOTES | fzf | xargs -r nvim; }

new(){
   cd $HOME/storage/shared/Noter
   printf '\n\n%s\n%s' "[//]: # ($(date -Iseconds))"\
   "[//]: # (tags: [<++>])" |\
   nvim -c startinsert -c Goyo -- "$note"
}

case $1 in new) new ;; *) edit; esac
